# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - Myphp

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://safiacr.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_bbbd3590eafe409ebc37e60d06fcfe14 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_5a621a20b02846449119913a53811a17 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: safiacr.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_bbbd3590eafe409ebc37e60d06fcfe14 }}/safiacr.azurecr.io/my-php-app:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'Myphp'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_615faf46766445e78599c99732ff4fda }}
        images: 'safiacr.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_bbbd3590eafe409ebc37e60d06fcfe14 }}/safiacr.azurecr.io/my-php-app:${{ github.sha }}'